root: true

extends: semistandard

rules:
  indent:
    - error
    - 4

  no-unused-vars:
    - error
    - args: after-used

  # excpetions specified in:
  # - src/.eslintrc.yml
  # - spec/.eslintrc.yml
